import { useState, useEffect, useCallback } from 'react';
import { ConsultarubrosidolistApplicationService } from '@modules/rubros/application/service/ConsultarubrosidolistApplicationService';
import { ConsultarubrosidolistGatewayAdapter } from '@modules/rubros/infrastructure/output/adapter/api/ConsultarubrosidolistGatewayAdapter';
import { Consultarubrosidolist, CreateConsultarubrosidolist, UpdateConsultarubrosidolist, ConsultarubrosidolistFilter } from '@modules/rubros/domain/model/Consultarubrosidolist';

// ─── Inyección manual: Gateway Adapter → Application Service ───
const gatewayAdapter = new ConsultarubrosidolistGatewayAdapter();
const consultarubrosidolistService = new ConsultarubrosidolistApplicationService(gatewayAdapter);

/**
 * Custom Hook: useConsultarubrosidolist
 * Conecta la UI con el Application Service de consultaRubrosIdoList.zul.
 * Maneja estado de carga, errores y datos.
 * Generated by migration-flow | GeneratedAt: 2026-03-27
 */
export function useConsultarubrosidolist() {
  const [items, setItems] = useState<Consultarubrosidolist[]>([]);
  const [selectedItem, setSelectedItem] = useState<Consultarubrosidolist | null>(null);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const [totalElements, setTotalElements] = useState(0);
  const [totalPages, setTotalPages] = useState(0);
  const [page, setPage] = useState(0);

  const fetchAll = useCallback(async (params?: ConsultarubrosidolistFilter) => {
    try {
      setLoading(true);
      setError(null);
      const response = await consultarubrosidolistService.findAdvanced(params);
      setItems(response.items);
      setTotalElements(response.totalElements);
      setTotalPages(response.totalPages);
      setPage(response.currentPage || 0);
    } catch (err) {
      setError(err instanceof Error ? err.message : 'Error al cargar datos');
    } finally {
      setLoading(false);
    }
  }, []);

  const fetchById = useCallback(async (id: string) => {
    try {
      setLoading(true);
      setError(null);
      const item = await consultarubrosidolistService.findById(id);
      setSelectedItem(item);
      return item;
    } catch (err) {
      setError(err instanceof Error ? err.message : 'Error al cargar detalle');
      return null;
    } finally {
      setLoading(false);
    }
  }, []);

  const create = useCallback(async (request: CreateConsultarubrosidolist) => {
    try {
      setLoading(true);
      setError(null);
      const created = await consultarubrosidolistService.create(request);
      setItems(prev => [...prev, created]);
      return created;
    } catch (err) {
      setError(err instanceof Error ? err.message : 'Error al crear');
      throw err;
    } finally {
      setLoading(false);
    }
  }, []);

  const update = useCallback(async (id: string | number, request: UpdateConsultarubrosidolist) => {
    try {
      setLoading(true);
      setError(null);
      const updated = await consultarubrosidolistService.update(String(id), request);
      setItems(prev => prev.map(item => item.id === Number(id) ? updated : item));
      if (selectedItem?.id === Number(id)) {
        setSelectedItem(updated);
      }
      return updated;
    } catch (err) {
      setError(err instanceof Error ? err.message : 'Error al actualizar');
      throw err;
    } finally {
      setLoading(false);
    }
  }, [selectedItem]);

  const remove = useCallback(async (id: string | number) => {
    try {
      setLoading(true);
      setError(null);
      await consultarubrosidolistService.remove(String(id));
      setItems(prev => prev.filter(item => item.id !== Number(id)));
      if (selectedItem?.id === Number(id)) {
        setSelectedItem(null);
      }
    } catch (err) {
      setError(err instanceof Error ? err.message : 'Error al eliminar');
      throw err;
    } finally {
      setLoading(false);
    }
  }, [selectedItem]);

  const exportar = useCallback(async (filter: ConsultarubrosidolistFilter) => {
    try {
      setLoading(true);
      setError(null);
      const result = await consultarubrosidolistService.exportar(filter);
      return result;
    } catch (err) {
      setError(err instanceof Error ? err.message : 'Error al exportar');
      throw err;
    } finally {
      setLoading(false);
    }
  }, []);

  const cambiarEstado = useCallback(async (id: string | number, estado: string) => {
    try {
      setLoading(true);
      setError(null);
      const updated = await consultarubrosidolistService.cambiarEstado(String(id), estado);
      setItems(prev => prev.map(item => item.id === Number(id) ? updated : item));
      if (selectedItem?.id === Number(id)) {
        setSelectedItem(updated);
      }
      return updated;
    } catch (err) {
      setError(err instanceof Error ? err.message : 'Error al cambiar estado');
      throw err;
    } finally {
      setLoading(false);
    }
  }, [selectedItem]);

  useEffect(() => {
    fetchAll();
  }, [fetchAll]);

  return {
    items,
    selectedItem,
    loading,
    error,
    totalElements,
    totalPages,
    page,
    fetchAll,
    fetchById,
    create,
    update,
    remove,
    exportar,
    cambiarEstado,
    clearError: () => setError(null),
  };
}
