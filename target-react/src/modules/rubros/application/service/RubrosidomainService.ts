import { RubrosidomainGatewayPort } from '../port/output/RubrosidomainGatewayPort';
import { RubrosidomainResponse, RubrosidomainListResponse, CreateRubrosidomainRequest, UpdateRubrosidomainRequest, RubrosidomainFilterParams, DeleteResponseDTO } from '../../infrastructure/input/adapter/dto/RubrosidomainDto';

/**
 * Application Service: rubrosIDOMain.zul / rubrosIDOList.zul / rubrosIDODetail.zul (rubrosIDOMain_rubrosIDOList_rubrosIDODetail)
 * Implementa la lógica de aplicación invocando el Gateway Port (puerto de salida).
 * Controladores fuente: 5
 * Servicios fuente: 0
 * Generated by migration-flow | GeneratedAt: 2026-03-27
 *
 * Origen controlador:
 *   GET /api/v1/rubros-ido — Listar rubros IDO
 *   GET /api/v1/rubros-ido/{id} — Obtener rubro IDO por ID
 *   POST /api/v1/rubros-ido — Crear rubro IDO
 *   PUT /api/v1/rubros-ido/{id} — Actualizar rubro IDO
 *   DELETE /api/v1/rubros-ido/{id} — Eliminar rubro IDO
 */
export class RubrosidomainService {
  constructor(private readonly gateway: RubrosidomainGatewayPort) {}

  async findById(id: string): Promise<RubrosidomainResponse> {
    return this.gateway.findById(id);
  }

  async findAll(params?: RubrosidomainFilterParams): Promise<RubrosidomainListResponse> {
    return this.gateway.findAll(params);
  }

  async create(request: CreateRubrosidomainRequest): Promise<RubrosidomainResponse> {
    // TODO: Validaciones de negocio antes de crear
    return this.gateway.create(request);
  }

  async update(id: string, request: UpdateRubrosidomainRequest): Promise<RubrosidomainResponse> {
    // TODO: Validaciones de negocio antes de actualizar
    return this.gateway.update(id, request);
  }

  async remove(id: string): Promise<DeleteResponseDTO> {
    return this.gateway.remove(id);
  }

  // TODO: Agregar métodos de negocio adicionales que combinen llamadas al gateway
}
